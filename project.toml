[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ODE_Audio"
version = "0.0.1"
authors = [
  { name="Nelis Drost", email="nelis.drost@auckland.ac.nz" },
]
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]